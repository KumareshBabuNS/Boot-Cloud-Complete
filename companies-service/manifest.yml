---
applications:

- name: companies-service
  host: companies-service-${random-word}
  buildpack: java_buildpack
  memory: 1G 
  path: target/companies-service-0.0.1-SNAPSHOT.jar
  services:
   - service-registry
   - config-server
   - companies-db
